# OpenAPI Scaffold

- Source schema: `src/shared/openapi/schema.yaml`
- Generated outputs: `src/shared/generated/*`
- Generate with: `npm run openapi:generate`

When backend Swagger is ready, replace `schema.yaml` with real spec and rerun codegen.
